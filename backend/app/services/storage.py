def generate_signed_upload_url(filename: str):
    return f"https://storage.example.com/upload/{filename}"

def generate_signed_download_url(filename: str):
    return f"https://storage.example.com/download/{filename}"
